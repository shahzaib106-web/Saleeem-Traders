import { Skeleton } from "@/components/ui/Skeleton";
export default function ProductLoading() { return <div className="container section product-detail"><Skeleton height={520} /><Skeleton height={420} /></div>; }
